import 'package:flutter/material.dart';

class InitRegistrationScreen extends StatelessWidget {
  const InitRegistrationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
